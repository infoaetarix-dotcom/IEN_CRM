// Test stub for the `server-only` package, which throws when imported outside
// a React Server Component. In unit tests we import server modules directly,
// so we alias `server-only` to this no-op.
export {};
